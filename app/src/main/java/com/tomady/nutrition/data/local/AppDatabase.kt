package com.tomady.nutrition.data.local

// This file previously contained duplicate @Database-annotated classes
// (DietDatabase, FooDBDatabase) that were never used.
// The actual Room databases are:
//   - com.tomady.nutrition.data.AppDatabase (main database)
// The DAO wrappers are in:
//   - com.tomady.nutrition.data.local.diet.DietDatabase (wrapper, not a RoomDatabase)
//   - com.tomady.nutrition.data.local.foodb.FooDBLocalDatabase (wrapper)
